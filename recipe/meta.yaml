{% set name = "pyclesperanto-prototype" %}
{% set version = "0.23.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pyclesperanto_prototype-{{ version }}.tar.gz
  sha256: 3a8def84665d2d179f3c415a84d2182a9a66c67888452d987e0f742b87e0ea8c

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - matplotlib-base
    - numpy !=1.19.4
    - platformdirs
    - pyopencl
    - python >=3.6
    - scikit-image >=0.19.0
    - toolz
    - transforms3d

test:
  imports:
    - pyclesperanto_prototype
  commands:
    - pip list
    - pip check
  requires:
    - pip
    - pocl  # [not win]
    - oclgrind  # [win]

about:
  home: https://github.com/clEsperanto/pyclesperanto_prototype
  summary: GPU-accelerated image processing in python using OpenCL
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - goanpeca
    - haesleinhuepf
